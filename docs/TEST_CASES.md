# 一笔画问题测试用例

说明：

- 顶点输入使用界面里的 1 基编号。
- 欧拉路径通常不唯一，所以只要程序输出的路径满足“每条边恰好经过一次”即可。
- 若存在一笔画，边序列长度应等于 Q，顶点路径长度应等于 Q + 1。
- 重边测试时，要重点看边标签 e1、e2 是否被分别高亮，而不是只看端点。

## 用例 1：三角形，欧拉回路

目的：测试 0 个奇度顶点。

P = 3

Q = 3

边列表：

```text
1 2
2 3
3 1
```

预期：

- 存在欧拉回路。
- 奇度顶点数量为 0。
- 一种路径：1 -> 2 -> 3 -> 1。

## 用例 2：链式图，欧拉通路

目的：测试 2 个奇度顶点。

P = 4

Q = 3

边列表：

```text
1 2
2 3
3 4
```

预期：

- 存在欧拉通路。
- 奇度顶点为 1 和 4。
- 一种路径：1 -> 2 -> 3 -> 4。

## 用例 3：星形图，不存在一笔画

目的：测试奇度顶点数量大于 2。

P = 4

Q = 3

边列表：

```text
1 2
1 3
1 4
```

预期：

- 不存在一笔画。
- 奇度顶点数量为 4。

## 用例 4：两个独立三角形，不连通

目的：测试所有非孤立顶点不连通。

P = 6

Q = 6

边列表：

```text
1 2
2 3
3 1
4 5
5 6
6 4
```

预期：

- 不存在一笔画。
- 日志应提示存在非孤立顶点无法从 BFS 起点到达。

## 用例 5：有孤立点但非孤立部分连通，欧拉回路

目的：测试孤立点不影响欧拉判定。

P = 5

Q = 3

边列表：

```text
1 2
2 3
3 1
```

预期：

- 存在欧拉回路。
- 顶点 4、5 是孤立点，但不影响一笔画。
- 路径只经过 1、2、3。

## 用例 6：两条重边，欧拉回路

目的：测试重边 edgeId 是否正确区分。

P = 2

Q = 2

边列表：

```text
1 2
1 2
```

预期：

- 存在欧拉回路。
- 两个顶点度数都是 2。
- 路径应使用 e1 和 e2 两条不同的边。
- 一种路径：1 -> 2 -> 1。

## 用例 7：重边加普通边，欧拉通路

目的：测试多条同端点边与普通边混合时的路径构造。

P = 3

Q = 3

边列表：

```text
1 2
1 2
2 3
```

预期：

- 存在欧拉通路。
- 奇度顶点为 2 和 3。
- 路径应覆盖两条不同的 1-2 重边和一条 2-3 边。

## 用例 8：四边形加两条对角线，不存在一笔画

目的：测试连通但奇度点过多。

P = 4

Q = 6

边列表：

```text
1 2
2 3
3 4
4 1
1 3
2 4
```

预期：

- 不存在一笔画。
- 奇度顶点数量为 4。

## 用例 9：复杂连通欧拉回路

目的：测试 Hierholzer 能处理有分支的欧拉回路。

P = 5

Q = 8

边列表：

```text
1 2
2 3
3 1
1 4
4 5
5 1
2 4
2 4
```

预期：

- 存在欧拉回路。
- 所有顶点度数都是偶数。
- 边序列长度为 8，顶点路径长度为 9。

## 用例 10：复杂连通欧拉通路

目的：测试 Hierholzer 能处理非回路路径。

P = 5

Q = 7

边列表：

```text
1 2
2 3
3 1
3 4
4 5
5 3
2 4
```

预期：

- 存在欧拉通路。
- 奇度顶点为 2 和 4。
- 边序列长度为 7，顶点路径长度为 8。

## 用例 11：只有顶点没有边

目的：测试 Q = 0 的边界情况。

P = 4

Q = 0

边列表为空。

预期：

- 按空边图处理，存在空的欧拉回路。
- 路径为空或只显示起点均可接受，但动画不会播放边。

## 用例 12：输入边数不匹配

目的：测试输入校验。

P = 3

Q = 3

边列表：

```text
1 2
2 3
```

预期：

- 程序提示输入错误。
- 错误原因：Q = 3，但实际只有 2 条非空边。

## 用例 13：顶点编号越界

目的：测试输入校验。

P = 3

Q = 2

边列表：

```text
1 2
2 4
```

预期：

- 程序提示输入错误。
- 错误原因：顶点 4 超出 1 到 3 的范围。

## 用例 14：自环测试

目的：如果老师允许无向图自环，用它测试自环度数贡献为 2。

P = 2

Q = 2

边列表：

```text
1 1
1 2
```

预期：

- 存在欧拉通路。
- 顶点 1 的度数为 3，顶点 2 的度数为 1。
- 奇度顶点为 1 和 2。
